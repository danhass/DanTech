@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Google.Apis.Oauth2.v2.Data.Userinfo


        @if (Model != null)
        {
            <p>
                @{ <strong>UserName:</strong> <code>@Model.Name</code>
                <strong>Email:</strong>
                @if (Model.Email == null)
                {
                    <code>None</code>
                }
                else
                {
                    <code>@Model.Email</code>
                }

                }
            </p>
        }
        else
        {
            <p>No info</p>
        }

